<%= form_for(@sales_order_source) do |f| %>
  <% if @sales_order_source.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@sales_order_source.errors.count, "error") %> prohibited this sales_order_source from being saved:</h2>

      <ul>
      <% @sales_order_source.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.text_field :user_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
