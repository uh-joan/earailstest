<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @order_source.name %>
</p>

<p>
  <strong>User:</strong>
  <%= User.find(@order_source.user_id).name %>
</p>

<%= link_to 'Edit', edit_order_source_path(@order_source) %> |
<%= link_to 'Back', order_sources_path %>
