<%= form_for(@top_category) do |f| %>
  <% if @top_category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@top_category.errors.count, "error") %> prohibited this top_category from being saved:</h2>

      <ul>
      <% @top_category.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :shortDescription %><br>
    <%= f.text_field :shortDescription %>
  </div>
  <div class="field">
    <%= f.label :enabled %><br>
    <%= f.check_box :enabled %>
  </div>
  <div class="field">
    <%= f.label :deleted %><br>
    <%= f.check_box :deleted %>
  </div>
  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.text_field :user_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
