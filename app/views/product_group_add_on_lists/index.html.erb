<p id="notice"><%= notice %></p>

<h1>Listing Product Group Add On Lists</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>**Displayindex</th>
      <th>**Logicindex</th>
      <th>**Required</th>
      <th>**Product group</th>
      <th>**User</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @product_group_add_on_lists.each do |product_group_add_on_list| %>
      <tr>
        <td><%= product_group_add_on_list.name %></td>
        <td><%= product_group_add_on_list.displayIndex %></td>
        <td><%= product_group_add_on_list.logicIndex %></td>
        <td><%= product_group_add_on_list.required %></td>
        <td><%= ProductGroup.find(product_group_add_on_list.product_group_id) %></td>
        <td><%= User.find(product_group_add_on_list.user_id) %></td>
        <td><%= link_to 'Show', product_group_add_on_list %></td>
        <td><%= link_to 'Edit', edit_product_group_add_on_list_path(product_group_add_on_list) %></td>
        <td><%= link_to 'Destroy', product_group_add_on_list, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Product group add on list', new_product_group_add_on_list_path %>
