<p id="notice"><%= notice %></p>

<h1>Listing Product Item Sizes</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Product item</th>
      <th>User</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @product_item_sizes.each do |product_item_size| %>
      <tr>
        <td><%= product_item_size.name %></td>
        <td><%= ProductItem.find(product_item_size.product_item_id).name %></td>
        <td><%= User.find(product_item_size.user_id).name %></td>
        <td><%= link_to 'Show', product_item_size %></td>
        <td><%= link_to 'Edit', edit_product_item_size_path(product_item_size) %></td>
        <td><%= link_to 'Destroy', product_item_size, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Product item size', new_product_item_size_path %>
