<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @user_type.name %>
</p>

<p>
  <strong>User:</strong>
  <%= User.find(@user_type.user_id).name %>
</p>

<%= link_to 'Edit', edit_user_type_path(@user_type) %> |
<%= link_to 'Back', user_types_path %>
